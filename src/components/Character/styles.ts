import styled from "styled-components";
import { Button } from "@mui/material";

export const StyledPaper = styled(Button)`
    padding: 8px;
    margin-bottom: 8px !important;
    width: 100%;
`
