import { Paper, Typography } from "@mui/material";

const Characters = () => {
  return (
    <Paper elevation={3} style={{ padding: "10px" }}>
      <Typography>Luke Skywalker</Typography>
    </Paper>
  );
};

export default Characters;
